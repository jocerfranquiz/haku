# Glossary — terms I'm learning while building `haku`

Define each term **in my own words** the first time I genuinely need it
during a build step. Don't pre-fill from textbooks. The act of writing
is the lesson.

Format per entry:

> ### Term
> **First seen in:** step N, §X.Y
> One- or two-sentence definition in my own words. A small example or
> intuition pump. If something is still fuzzy, write the open question
> at the end.

---

## Pending (to fill as they come up)

The following terms appear in DESIGN.md. Leave them empty until I hit
them in a build step — filling all of them now is just copying the
internet, not learning.

- [ ] **Tokenizer / BPE / WordPiece** (step 1, §14)
- [ ] **Token vs character vs byte** (step 1, §13)
- [ ] **Chunking with overlap** (step 2, §13)
- [ ] **WAL mode (Write-Ahead Logging)** (step 3, §12.5, §23)
- [ ] **SQLite extension loading** (step 3, §7)
- [ ] **Per-file transaction / atomic commit** (step 3, §12.6)
- [ ] **Foreign key cascade delete** (step 3, §19)
- [ ] **FTS5 / external content tables** (step 7, §19)
- [ ] **`unicode61` tokenizer / `remove_diacritics`** (step 7, §16.1)
- [ ] **ONNX / ONNX Runtime / CPU Execution Provider** (step 4, §12.4)
- [ ] **Dynamic vs static quantization** (step 4, §12.4)
- [ ] **INT8 quantization** (step 4, §12.4)
- [ ] **Embedding / dense vector** (step 4, §12.4)
- [ ] **Mean pooling** (step 4, §12.4)
- [ ] **L2 normalization** (step 4, §12.4)
- [ ] **Cosine similarity** (step 6, §16.1)
- [ ] **ANN (approximate nearest neighbor)** (step 6, §16)
- [ ] **`recall@k` / `NDCG@k`** (step 4, §12.4 note on quantization quality)
- [ ] **BM25** (step 7, §16.1)
- [ ] **RRF (Reciprocal Rank Fusion)** (step 7, §16.2)
- [ ] **Bi-encoder vs cross-encoder** (step 8, §16.4)
- [ ] **Reranking / pointwise scoring** (step 8, §16.4)
- [ ] **`fcntl.flock` / advisory locking** (step 5, §12.5)
- [ ] **JSONL (JSON Lines)** (step 5, §22)
- [ ] **MDX (Markdown + JSX)** (step 2, §12.0)
- [ ] **AGPL network clause** (step 10, §2)

---

## Defined

*(empty for now — entries land here as I work through the steps)*
